# Restore Points

Restore-point tags and pinned failsafe artifacts are intentionally retired for this repository variant.

Current policy:
- no restore-point tags are maintained in this project;
- no pinned failsafe image is maintained in `failsafe/`.
